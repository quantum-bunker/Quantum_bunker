/// <reference types="vite/client" />

// Injected by Vite `define` (see vite.config.ts). package.json is the single
// source of truth for the version; these replace hardcoded strings in the UI.
declare const __APP_VERSION__: string;
declare const __BUILD_COMMIT__: string;
